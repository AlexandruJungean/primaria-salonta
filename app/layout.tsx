// Root layout - minimal, just for the HTML structure
// The actual layout with header/footer is in [locale]/layout.tsx

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

